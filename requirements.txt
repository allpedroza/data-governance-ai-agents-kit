# Minimal base requirements — for full agent support use:
#   pip install -e ".[all]"
#
# Or pick specific agent groups:
#   pip install -e ".[lineage,rag]"
#
# See pyproject.toml for the full list of optional groups.

streamlit>=1.32.0
openai>=1.0.0
python-dotenv>=1.0.0
pyyaml>=6.0
requests>=2.31.0
